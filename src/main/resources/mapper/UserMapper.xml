<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cuava_cache.sys.mapper.UserMapper">
    <insert id="addUser">

    </insert>

    <select id="getUsers" resultType="com.example.cuava_cache.sys.domain.User">

    </select>
    <select id="getUsersByName" resultType="com.example.cuava_cache.sys.domain.User">
           select * from user
    </select>
</mapper>
